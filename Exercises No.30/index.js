let usernames = ['admin', 'cordinator', 'author', 'manager', 'analyst'];
for (let user of usernames) {
    if (user == 'admin') {
        console.log(`Hey welcome Mr/Mrs ${user}❤, Do you want see some technical stuff? `);
    }
    else {
        console.log(`Hey welcome Mr/Mrs ${user}❤ `);
    }
}
export {};
